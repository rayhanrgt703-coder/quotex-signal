services:
  - type: web
    name: smart-pro-signal-v4-finnhub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: FINNHUB_API_KEY
        sync: false
